// ltl-otel-spans/main.ltl
//
// OpenTelemetry-compatible span emitter over OTLP/HTTP.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-otel-spans v0.1.0")
    io::println("OpenTelemetry-compatible span emitter over OTLP/HTTP.")
    0
}
